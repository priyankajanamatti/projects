# Project

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 7.3.1.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

# Project description
When the application loads ,list of github users will be displayed.
 
 On click of view , it willbe navigated to viewuser page,wherein list of repositories of a particular user will be displayed.
   
   Also there is a search option to find a particular user by their name.